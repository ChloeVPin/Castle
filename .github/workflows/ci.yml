name: ci

on:
  push:
    branches: [main, master]
  pull_request:

jobs:
  check:
    name: fmt + clippy + test (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [macos-latest, ubuntu-latest, windows-latest]

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      # ---- Ubuntu: clang ≥ 18 via llvm.org apt ----
      - name: Install clang ≥ 18 (Ubuntu)
        if: matrix.os == 'ubuntu-latest'
        run: |
          set -e
          CODENAME=$(lsb_release -cs)
          wget -qO - https://apt.llvm.org/llvm-snapshot.gpg.key | sudo gpg --dearmor -o /usr/share/keyrings/llvm-archive-keyring.gpg
          echo "deb [signed-by=/usr/share/keyrings/llvm-archive-keyring.gpg] http://apt.llvm.org/${CODENAME}/ llvm-toolchain-${CODENAME}-18 main" | sudo tee /etc/apt/sources.list.d/llvm.list
          sudo apt-get update
          sudo apt-get install -y clang-18 clang++-18 clang-format-18 clang-tidy-18 cmake
          sudo ln -sf /usr/bin/clang-18 /usr/local/bin/clang
          sudo ln -sf /usr/bin/clang++-18 /usr/local/bin/clang++
          sudo ln -sf /usr/bin/clang-format-18 /usr/local/bin/clang-format
          sudo ln -sf /usr/bin/clang-tidy-18 /usr/local/bin/clang-tidy
          clang --version
          clang++ --version

      # ---- macOS: clang is already present (Xcode CLT) ----
      - name: Verify clang on macOS
        if: matrix.os == 'macos-latest'
        run: |
          clang --version
          clang++ --version

      # ---- Windows: LLVM via chocolatey ----
      - name: Install LLVM (Windows)
        if: matrix.os == 'windows-latest'
        run: |
          choco install llvm --version=18.1.8 --yes --no-progress
          echo "$env:ProgramFiles\LLVM\bin" | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append
          # Also install cmake (not bundled with llvm choco package)
          choco install cmake --yes --no-progress
        shell: pwsh

      - name: Verify clang on Windows
        if: matrix.os == 'windows-latest'
        run: |
          clang --version
          clang++ --version
        shell: pwsh

      # ---- Rust checks (all OS) ----
      - name: cargo fmt --check
        run: cargo fmt -- --check

      - name: cargo clippy
        run: cargo clippy --all-targets -- -D warnings

      - name: cargo test
        run: cargo test

      # ---- Live round-trip: clang backend (all OS) ----
      - name: Sample round-trip (clang backend)
        run: |
          cargo install --path . --force
          castle new ci-demo
          cd ci-demo
          castle build
          castle run
          castle check
          castle test
          castle clean --force
        shell: bash

      # ---- Live round-trip: cmake backend + fmt + tidy ----
      # fmt/tidy require clang-format / clang-tidy. The Ubuntu and Windows
      # install steps above put them on PATH, but macOS Xcode CLT only
      # provides clang/clang++ (not the extra LLVM tools), so this step is
      # gated to non-macOS.
      - name: Sample round-trip (cmake backend + fmt + tidy)
        if: matrix.os != 'macos-latest'
        run: |
          castle new ci-cmake --cmake
          cd ci-cmake
          castle build --backend cmake
          castle run
          castle fmt
          castle tidy
          castle clean --force
        shell: bash

  # ── Cross-compile check ─────────────────────────────────────────────
  # Verifies Castle type-checks for every tier-1 platform (including
  # ARM) without needing platform linkers.  `cargo check` only needs the
  # target's std via `rustup target add` — no SDK or cross-linker required.
  cross-check:
    name: cross-check (${{ matrix.target }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        target:
          - x86_64-pc-windows-gnu
          - x86_64-apple-darwin
          - aarch64-apple-darwin
          - aarch64-unknown-linux-gnu
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Add target
        run: rustup target add ${{ matrix.target }}

      - name: cargo check --all-targets
        run: cargo check --all-targets --target ${{ matrix.target }}
